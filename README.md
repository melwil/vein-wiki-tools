# Vein Wiki Bot

Sync information to vein.wiki.gg

## Setup for running

Copy the example files by removing `example-` and fill them out.
Make sure to navigate to `src/vein_wiki_tools` before running scripts.
Login with `poetry run pwb login`

## Running scripts

`poetry run python <script>`